# Linux System Monitor Desktop

This workspace contains a Tauri + React MVP shell for CPU/RAM/swap/disk/network metrics and process control.

Run tests:

- Rust: `cd apps/desktop/src-tauri && cargo test`
- Frontend: `cd apps/desktop && npm run test`
